calendar:
  logger:
    level: "DEBUG"

  server:
    type: "http"
    host: "0.0.0.0"
    port: "8080"
    timeOut: 30

  storage:
    type: "postgres"
    dsn: "postgres://calendar_user:calendar@postgres:5432/calendar?sslmode=disable"
