apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: calendar-ingress
spec:
  rules:
  - host: my.calendar.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: calendar
            port:
              number: 8888
